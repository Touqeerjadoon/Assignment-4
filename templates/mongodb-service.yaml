apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongodb.service.name | default "mongodb-service" }}
spec:
  selector:
    app: mongodb
  ports:
  - protocol: TCP
    port: {{ .Values.mongodb.service.port | default 27017 }}
    targetPort: {{ .Values.mongodb.service.port | default 27017 }}